# MedVoice AI — Frontend

Interface React + TypeScript pour MedVoice AI.

## Run Locally

**Prerequisites:** Node.js, backend running on `localhost:8000`

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Open `http://localhost:3000`
